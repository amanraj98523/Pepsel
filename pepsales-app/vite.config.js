// Import the defineConfig function from Vite
import { defineConfig } from 'vite';

// Export the configuration using defineConfig
export default defineConfig({
  server: {
    port: 3000,  // Set the port to whatever you'd like
  },
});
